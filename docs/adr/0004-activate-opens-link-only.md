# Activate opens link, never guid

`guid` is identity. `isPermaLink` is often wrong or a tag URI. We open `link` in the default browser and refuse to treat `guid` as a URL. Rows with only a `guid` stay listable so unread can still be cleared by mark read.
