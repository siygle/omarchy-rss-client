# One instance, id `io.github.siygle.omarchy-rss-client`

Third-party plugins cannot use `omarchy.*`. Reverse-DNS from the GitHub owner is stable if the repo is renamed. `allowMultiple` would mean two feed lists and two read sets; v1 does not need that. Poll default 15 minutes and floor 5 minutes so a settings typo cannot hammer publishers.
